import styled from 'styled-components';

export const Container = styled.div`
  display: block;
  width: 2.8rem;
  height: 2.8rem;
`;

export const Photo = styled.img`
  overflow: hidden;
  object-fit: cover;
  filter: grayscale(1);
`;
