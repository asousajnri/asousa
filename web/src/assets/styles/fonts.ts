import { css } from "styled-components";

import fontImport from "../../utils/font-import";

export default css`
  /* FONT FAMILY - CIRCULAR STD */
  ${fontImport("Circular Std", "circular-std", "900", "normal")}
  ${fontImport("Circular Std", "circular-std", "900-italic", "italic")}
  ${fontImport("Circular Std", "circular-std", "700", "normal")}
  ${fontImport("Circular Std", "circular-std", "700-italic", "italic")}
  ${fontImport("Circular Std", "circular-std", "500", "normal")}
  ${fontImport("Circular Std", "circular-std", "500-italic", "italic")}
  ${fontImport("Circular Std", "circular-std", "400", "normal")}
  ${fontImport("Circular Std", "circular-std", "400-italic", "italic")}
`;
