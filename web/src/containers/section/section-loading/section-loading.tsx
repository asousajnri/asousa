import React from 'react';

import {
  StyledSection,
  StyledSectionTitle,
} from './section-loading-styles';

const SectionLoading: React.FC = ({ children }) => (
  <StyledSection>
    <StyledSectionTitle />
  </StyledSection>
);

export default SectionLoading;
